---
phase: 14-ability-data-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - Import/Pipeline.lua
  - Data/MagistersTerrace.lua
  - Data/SeatoftheTriumvirate.lua
  - Data/MurderRow.lua
  - TerriblePackWarnings.toc
  - scripts/install.bat
autonomous: true
gap_closure: true
requirements: [DATA-13, DATA-14, DATA-15]

must_haves:
  truths:
    - "RestoreFromSaved rebuilds packs from AbilityDB + skillConfig instead of copying stale serialized data"
    - "Murder Row is completely removed from the addon"
    - "Magisters Terrace and Seat of the Triumvirate have full AbilityDB data extracted from MDT"
  artifacts:
    - path: "Import/Pipeline.lua"
      provides: "Fixed RestoreFromSaved, Murder Row removed from DUNGEON_IDX_MAP"
      contains: "BuildPack"
    - path: "Data/MagistersTerrace.lua"
      provides: "Full AbilityDB entries for all MT mobs with spells"
      contains: "ns.AbilityDB"
    - path: "Data/SeatoftheTriumvirate.lua"
      provides: "Full AbilityDB entries for all SotT mobs with spells"
      contains: "ns.AbilityDB"
  key_links:
    - from: "Import/Pipeline.lua:RestoreFromSaved"
      to: "Import/Pipeline.lua:BuildPack"
      via: "rebuild from saved preset instead of copying stale packs"
      pattern: "BuildPack"
---

<objective>
Fix RestoreFromSaved to rebuild packs from current skillConfig, remove Murder Row, and populate Magisters Terrace and Seat of the Triumvirate AbilityDB data files from MDT source.

Purpose: Close UAT gaps 2 and 3 — enabling abilities must survive /reload, Murder Row must be removed, and MT/SotT must have real spell data.
Output: Fixed Pipeline.lua, two populated Data files, Murder Row removed from TOC/install.bat.
</objective>

<execution_context>
@C:/Users/jonat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jonat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ability-data-foundation/14-01-SUMMARY.md
@.planning/phases/14-ability-data-foundation/14-02-SUMMARY.md
@.planning/phases/14-ability-data-foundation/14-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RestoreFromSaved and remove Murder Row from Pipeline.lua</name>
  <files>Import/Pipeline.lua</files>
  <read_first>Import/Pipeline.lua</read_first>
  <action>
Two changes to Import/Pipeline.lua:

1. **Remove Murder Row from DUNGEON_IDX_MAP** (around line 16):
   Delete the line `[160] = { key = "murder_row", name = "Murder Row" },` from the DUNGEON_IDX_MAP table.

2. **Fix Import.RestoreFromSaved** (around line 186-196):
   The current implementation copies stale serialized packs:
   ```lua
   ns.PackDatabase["imported"] = saved.packs
   ```
   Replace with a rebuild that uses the saved preset data plus current skillConfig.
   The saved data in `ns.db.importedRoute` has `.dungeonIdx` and `.preset` (the decoded MDT preset).
   Change RestoreFromSaved to:

   ```lua
   function Import.RestoreFromSaved()
       if not ns.db.importedRoute then return end
       local saved = ns.db.importedRoute

       -- Rebuild packs from saved preset + current skillConfig (not stale serialized packs)
       if saved.preset and saved.dungeonIdx then
           local result = Import.RunFromPreset(saved.preset)
           if result then
               print(string.format("|cff00ccffTPW|r Restored: %s - %d pulls", saved.dungeonName, #(ns.PackDatabase["imported"] or {})))
           else
               print("|cff00ccffTPW|r Failed to rebuild saved route")
           end
       else
           -- Legacy fallback: copy saved packs directly (pre-rebuild format)
           ns.PackDatabase["imported"] = saved.packs
           print(string.format("|cff00ccffTPW|r Restored: %s - %d pulls", saved.dungeonName, #saved.packs))
           ns.CombatWatcher:SelectDungeon("imported")
           if ns.PackUI and ns.PackUI.Refresh then ns.PackUI:Refresh() end
       end
   end
   ```

   IMPORTANT: Also check if `Import.RunFromPreset` already calls `CombatWatcher:SelectDungeon` and `PackUI:Refresh`. If it does, no duplication needed in the rebuild path. If not, add those calls after the rebuild.

   Also check if the current import flow saves `preset` and `dungeonIdx` in `ns.db.importedRoute`. Look at `Import.RunFromString` or `Import.RunFromPreset` to see what gets saved. If `preset` is not currently saved, add it:
   - In the function that saves to `ns.db.importedRoute`, ensure `preset = result` (the decoded preset) and `dungeonIdx = dungeonIdx` are included alongside the existing `packs` and `dungeonName` fields.
  </action>
  <verify>
    <automated>grep -n "murder_row\|Murder Row" Import/Pipeline.lua | wc -l</automated>
    Output must be 0 (Murder Row completely removed).
    Also: `grep -n "RunFromPreset\|BuildPack" Import/Pipeline.lua` must show RestoreFromSaved calling rebuild logic.
  </verify>
  <acceptance_criteria>
    - `grep -c "murder_row" Import/Pipeline.lua` returns 0
    - `grep -c "Murder Row" Import/Pipeline.lua` returns 0
    - `grep "RestoreFromSaved" Import/Pipeline.lua` shows function exists
    - `grep "RunFromPreset\|BuildPack" Import/Pipeline.lua` appears inside RestoreFromSaved
    - `grep "saved.packs" Import/Pipeline.lua` appears only in legacy fallback branch
  </acceptance_criteria>
  <done>Murder Row removed from DUNGEON_IDX_MAP. RestoreFromSaved rebuilds packs from saved preset + current skillConfig instead of copying stale data. Legacy fallback preserved for old saved data.</done>
</task>

<task type="auto">
  <name>Task 2: Populate Magisters Terrace and Seat of the Triumvirate data files</name>
  <files>Data/MagistersTerrace.lua, Data/SeatoftheTriumvirate.lua</files>
  <read_first>Data/MagistersTerrace.lua, Data/SeatoftheTriumvirate.lua, Data/AlgetharAcademy.lua</read_first>
  <action>
Replace both stub files with full AbilityDB entries extracted from MDT source. Follow the same pattern as Data/AlgetharAcademy.lua (read it first for schema reference).

Schema per mob:
```lua
ns.AbilityDB[npcID] = {
    mobClass = "WARRIOR",
    abilities = {
        { spellID = NNNNN, defaultEnabled = false },
    },
}
```

**Data/MagistersTerrace.lua** — Populate with these mobs (skip mobs without spells: Animated Codex 234089, Vigilant Librarian 234067, Void-Infused Brightscale 241354):

```
-- Trash mobs
Arcane Magister (232369): spellIDs 468962, 468966, 1245046
Blazing Pyromancer (251861): spellIDs 1254294, 1254301, 1254336, 1254338
Runed Spellbreaker (240973): spellIDs 1244907, 1283901, 1283905
Voidling (234069): spellIDs 1248229, 1255434
Hollowsoul Shredder (234065): spellIDs 1227020, 1248229
Dreaded Voidwalker (234064): spellIDs 1248229, 1248327
Shadowrift Voidcaller (234068): spellIDs 1217087, 1255462, 1265977
Devouring Tyrant (234066): spellIDs 1248138, 1248219, 1248229, 1264687
Void Infuser (249086): spellIDs 1245068, 1248229, 1264693
Brightscale Wyrm (232106): spellIDs 467068, 1254595
Arcane Sentry (234062): spellIDs 473258, 1282050, 1282051, 1282053, 1282055
Sunblade Enforcer (234124): spellIDs 1252910, 1253224, 1265561
Lightward Healer (234486): spellIDs 1254306, 1255187
Unstable Voidling (255376): spellIDs 1248229, 1264951
Hollowsoul Shredder (257447): spellIDs 1227020, 1248229
Spellwoven Familiar (259387): spellIDs 1279994, 1279995

-- Bosses
Arcanotron Custos (231861): spellIDs 474345, 474496, 1214038, 1214081, 1243905
Seranel Sunlash (231863): spellIDs 1224903, 1225015, 1225135, 1225193, 1225201, 1225205, 1225792, 1225796, 1246446, 1248689, 1271317
Gemellus (231864): spellIDs 1223847, 1223936, 1224104, 1224299, 1224401, 1253707, 1253709, 1284954, 1284958
Degentrius (231865): spellIDs 1215087, 1215897, 1269631, 1271066, 1280113, 1280119, 1284627, 1284633
Gemellus (239636): spellIDs 1223936, 1224104, 1224299, 1224401, 1253707, 1253709, 1284954, 1284958
Celestial Drifter (241397): spellIDs 1248015
```

**Data/SeatoftheTriumvirate.lua** — Populate with these mobs (skip mobs without spells: Bound Voidcaller 122412, Coalesced Void 122716, Depravation Wave Stalker 255551):

```
-- Trash mobs
Merciless Subjugator (124171): spellIDs 1262506, 1262509, 1277343
Rift Warden (122571): spellIDs 1264505, 1264532, 1264569, 1280330
Ruthless Riftstalker (122413): spellIDs 1262519, 1277339, 1277340
Ravenous Umbralfin (255320): spellIDs 1264670, 1264678
Umbral War-Adept (122421): spellIDs 1269183, 1280326
Dire Voidbender (122404): spellIDs 1262526, 1262527
Void-Infused Destroyer (252756): spellIDs 1262335, 1262429, 1262441
Grand Shadow-Weaver (122423): spellIDs 1262508, 1264286
Famished Broken (122322): spellIDs 1269468, 1269469, 1269470
Shadowguard Champion (122403): spellIDs 1262517, 1264036
Dark Conjurer (122405): spellIDs 1262510, 1262522, 1262523
Umbral Tentacle (122827): spellIDs 249082, 1268733
Void Tentacle (256424): spellIDs 1269081

-- Bosses
Viceroy Nezhar (122056): spellIDs 244750, 246913, 1263528, 1263529, 1263532, 1263538, 1263542
Zuraal the Ascended (122313): spellIDs 244579, 1263282, 1263297, 1263399, 1263440, 1263484, 1263492, 1263494, 1268916
Saprish (122316): spellIDs 246943, 1263523, 1280065
Darkfang (122319): spellIDs 245742, 246943
L'ura (124729): spellIDs 1264159, 1264196, 1265419, 1265420, 1265421, 1265426, 1265463, 1265689, 1265999, 1266001, 1266003, 1267207, 1267274, 1268598, 1268646, 1268647
Shadewing (125340): spellIDs 246943, 248829, 248830, 248831
```

Add comments with mob names above each ns.AbilityDB entry for readability (matching AlgetharAcademy.lua pattern).
  </action>
  <verify>
    <automated>grep -c "ns.AbilityDB" Data/MagistersTerrace.lua && grep -c "ns.AbilityDB" Data/SeatoftheTriumvirate.lua</automated>
    MagistersTerrace should have 22 entries (16 trash + 6 bosses). SeatoftheTriumvirate should have 19 entries (13 trash + 6 bosses).
  </verify>
  <acceptance_criteria>
    - `grep -c "ns.AbilityDB\[" Data/MagistersTerrace.lua` returns 22
    - `grep -c "ns.AbilityDB\[" Data/SeatoftheTriumvirate.lua` returns 19
    - `grep -c "defaultEnabled = false" Data/MagistersTerrace.lua` is greater than 40
    - `grep -c "defaultEnabled = false" Data/SeatoftheTriumvirate.lua` is greater than 40
    - `grep -c "mobClass" Data/MagistersTerrace.lua` returns 22
    - `grep -c "mobClass" Data/SeatoftheTriumvirate.lua` returns 19
    - Both files start with `local addonName, ns = ...` and `ns.AbilityDB = ns.AbilityDB or {}`
  </acceptance_criteria>
  <done>Both dungeon data files populated with all MDT-sourced spellIDs. All entries use mobClass=WARRIOR and defaultEnabled=false per project conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Murder Row from TOC and install.bat, delete Data/MurderRow.lua</name>
  <files>TerriblePackWarnings.toc, scripts/install.bat, Data/MurderRow.lua</files>
  <read_first>TerriblePackWarnings.toc, scripts/install.bat</read_first>
  <action>
1. **Delete Data/MurderRow.lua** — remove the file entirely.

2. **TerriblePackWarnings.toc** — Remove the line `Data\MurderRow.lua` from the file listing.

3. **scripts/install.bat** — Remove the line `copy /Y "%SOURCE%Data\MurderRow.lua" "%DEST%\Data\"` from the script.
  </action>
  <verify>
    <automated>test ! -f Data/MurderRow.lua && ! grep -q "MurderRow" TerriblePackWarnings.toc && ! grep -q "MurderRow" scripts/install.bat && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Data/MurderRow.lua` returns non-zero (file does not exist)
    - `grep -c "MurderRow" TerriblePackWarnings.toc` returns 0
    - `grep -c "MurderRow" scripts/install.bat` returns 0
  </acceptance_criteria>
  <done>Murder Row completely removed: data file deleted, TOC entry removed, install.bat copy command removed.</done>
</task>

</tasks>

<verification>
1. `grep -c "murder_row\|Murder Row" Import/Pipeline.lua` returns 0
2. RestoreFromSaved calls RunFromPreset or BuildPack instead of copying saved.packs
3. Data/MurderRow.lua does not exist
4. MurderRow not referenced in TOC or install.bat
5. `grep -c "ns.AbilityDB\[" Data/MagistersTerrace.lua` returns 22
6. `grep -c "ns.AbilityDB\[" Data/SeatoftheTriumvirate.lua` returns 19
</verification>

<success_criteria>
- RestoreFromSaved rebuilds packs from current skillConfig so ability enable/disable changes survive /reload
- Murder Row completely removed from all addon files
- Magisters Terrace has 22 npcID entries with full spell data from MDT
- Seat of the Triumvirate has 19 npcID entries with full spell data from MDT
</success_criteria>

<output>
After completion, create `.planning/phases/14-ability-data-foundation/14-03-SUMMARY.md`
</output>
