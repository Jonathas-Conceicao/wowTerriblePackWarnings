---
phase: 01-foundation-and-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TerriblePackWarnings.toc
  - Core.lua
  - Data/WindrunnerSpire.lua
autonomous: true
requirements:
  - FOUND-01
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Addon appears in the WoW addon list and loads without Lua errors"
    - "Load message prints in chat on first login or /reload"
    - "PackDatabase contains one pack entry with one mob and one ability"
    - "The ability entry has both first_cast and cooldown fields"
    - "/reload completes without taint errors or secret value violations"
  artifacts:
    - path: "TerriblePackWarnings.toc"
      provides: "Addon manifest with dual interface version, file load order, SavedVariables"
      contains: "## Interface: 120000, 120001"
    - path: "Core.lua"
      provides: "Addon namespace init, event frame, ADDON_LOADED handler, SavedVariables init, slash command stub"
      min_lines: 25
    - path: "Data/WindrunnerSpire.lua"
      provides: "Pack database entry for Windrunner Spire pack 1 with Spellguard Magus ability data"
      contains: "windrunner_spire_pack_1"
  key_links:
    - from: "Core.lua"
      to: "Data/WindrunnerSpire.lua"
      via: "ns.PackDatabase initialized at module scope in Core.lua before Data file executes"
      pattern: "ns\\.PackDatabase\\s*="
    - from: "TerriblePackWarnings.toc"
      to: "Core.lua"
      via: "TOC file load order lists Core.lua before Data files"
      pattern: "Core\\.lua"
    - from: "TerriblePackWarnings.toc"
      to: "Data/WindrunnerSpire.lua"
      via: "TOC file load order lists Data file after Core.lua"
      pattern: "Data\\\\WindrunnerSpire\\.lua"
---

<objective>
Create the loadable WoW Midnight addon skeleton with correct TOC, namespace, event handling, and the pack/ability database for Windrunner Spire.

Purpose: This is the core addon that must load in WoW without errors and expose queryable pack data -- the foundation everything else builds on.
Output: Three files (TOC, Core.lua, Data/WindrunnerSpire.lua) that form a complete, loadable addon.
</objective>

<execution_context>
@C:/Users/jonat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jonat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data/01-CONTEXT.md
@.planning/phases/01-foundation-and-data/01-RESEARCH.md

<interfaces>
<!-- Reference project patterns to mirror exactly (with name substitutions) -->

From TerribleBuffTracker/TerribleBuffTracker.toc:
```
## Interface: 120000, 120001
## Title: TerribleBuffTracker
## Version: @project-version@
## SavedVariables: TerribleBuffTrackerDB
```

From TerribleBuffTracker/Core.lua:
```lua
local addonName, ns = ...
-- event frame pattern with RegisterEvent/UnregisterEvent
-- ADDON_LOADED guard: if name ~= addonName then return end
-- SavedVariables nil-check initialization
-- colored print load message
-- SLASH_ global + SlashCmdList registration
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TOC manifest and Core.lua addon skeleton</name>
  <files>TerriblePackWarnings.toc, Core.lua</files>
  <action>
Create TerriblePackWarnings.toc mirroring TerribleBuffTracker format exactly (with name substitutions):
- `## Interface: 120000, 120001` (dual version per locked decision)
- `## Title: TerriblePackWarnings`
- `## Notes: Dungeon trash pack ability timers for Mythic+`
- `## Author: Jonathas-Conceicao`
- `## Version: @project-version@` (BigWigsMods/packager replaces this on release)
- `## URL: https://github.com/Jonathas-Conceicao/TerriblePackWarnings`
- `## Category: Combat`
- `## SavedVariables: TerriblePackWarningsDB`
- File load order: `Core.lua` then `Data\WindrunnerSpire.lua`
- Do NOT include X-Curse-Project-ID or X-Wago-ID (no project IDs yet)
- Do NOT include IconTexture (no icon asset yet)

Create Core.lua following TerribleBuffTracker/Core.lua pattern:
- `local addonName, ns = ...` namespace (per locked decision: no global TPW table)
- `ns.PackDatabase = ns.PackDatabase or {}` at MODULE SCOPE (not inside event handler -- critical: data files execute at load time before ADDON_LOADED fires, so PackDatabase must exist before Data/WindrunnerSpire.lua runs)
- Event frame with `CreateFrame("Frame")` registering ADDON_LOADED and PLAYER_ENTERING_WORLD
- ADDON_LOADED handler: guard with `if name ~= addonName then return end`, initialize SavedVariables (`if not TerriblePackWarningsDB then TerriblePackWarningsDB = {} end`), store `ns.db = TerriblePackWarningsDB`, print colored load message: `|cff00ccffTerriblePackWarnings|r loaded. Type |cff00ff00/tpw|r to configure.`, then `self:UnregisterEvent("ADDON_LOADED")`
- PLAYER_ENTERING_WORLD handler: placeholder comment for future display init, then `self:UnregisterEvent("PLAYER_ENTERING_WORLD")`
- Slash command: `SLASH_TERRIBLEPACKWARNINGS1 = "/tpw"` and `SlashCmdList["TERRIBLEPACKWARNINGS"] = function() end` (stub for Phase 3)

NOTE on success criterion verification: The ROADMAP says `/run print(TPW.PackDatabase)` but the locked decision prohibits a global TPW table. The PackDatabase is accessible via the ns namespace which is not directly reachable from `/run`. For in-game verification, use `/run print(TerriblePackWarningsDB)` to confirm SavedVariables loads (prints "table: 0x..."), and visually confirm the load message prints in chat. The pack data itself is verified by code inspection of Data/WindrunnerSpire.lua. If the executor determines a simple debug helper is needed, they may add a `/tpw debug` subcommand that prints PackDatabase contents -- but do NOT create a global TPW table.
  </action>
  <verify>
    <automated>bash -c "test -f TerriblePackWarnings.toc && test -f Core.lua && grep -q '120000, 120001' TerriblePackWarnings.toc && grep -q 'ns.PackDatabase' Core.lua && grep -q 'TerriblePackWarningsDB' Core.lua && grep -q 'ADDON_LOADED' Core.lua && grep -q '/tpw' Core.lua && echo PASS || echo FAIL"</automated>
  </verify>
  <done>
- TerriblePackWarnings.toc exists with dual Interface version, @project-version@, SavedVariables declaration, and correct file load order
- Core.lua exists with ns namespace pattern, PackDatabase initialization at module scope, ADDON_LOADED/PLAYER_ENTERING_WORLD handlers with proper guards and unregistration, colored load message, and /tpw slash command stub
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Windrunner Spire pack data file</name>
  <files>Data/WindrunnerSpire.lua</files>
  <action>
Create Data/WindrunnerSpire.lua with the pack database entry for Windrunner Spire pack 1.

The file must:
- Start with `local addonName, ns = ...` (same namespace pattern as Core.lua)
- Write into `ns.PackDatabase["windrunner_spire_pack_1"]` (human-readable pack key per locked decision)
- Use the exact schema from the research:

```lua
ns.PackDatabase["windrunner_spire_pack_1"] = {
    displayName = "Windrunner Spire — Pack 1",
    mobs = {
        {
            name = "Spellguard Magus",
            npcID = 232113,
            abilities = {
                {
                    name = "Spellguard's Protection",
                    spellID = 1253686,
                    first_cast = 50,   -- seconds after pull
                    cooldown = 50,     -- repeat interval in seconds
                },
            },
        },
    },
}
```

Per locked decisions:
- Single mob: Spellguard Magus (NPC ID 232113)
- Single ability: Spellguard's Protection (Spell ID 1253686)
- first_cast = 50, cooldown = 50 (satisfies DATA-02: every ability has both fields)
- Schema supports multiple mobs and abilities per pack (future-ready) but v1 ships with one entry
- Dungeon file contains ONLY pack/mob/ability data -- no extra metadata
  </action>
  <verify>
    <automated>bash -c "test -f Data/WindrunnerSpire.lua && grep -q 'windrunner_spire_pack_1' Data/WindrunnerSpire.lua && grep -q 'first_cast' Data/WindrunnerSpire.lua && grep -q 'cooldown' Data/WindrunnerSpire.lua && grep -q '232113' Data/WindrunnerSpire.lua && grep -q '1253686' Data/WindrunnerSpire.lua && echo PASS || echo FAIL"</automated>
  </verify>
  <done>
- Data/WindrunnerSpire.lua exists with correct namespace pattern
- Pack entry uses human-readable key "windrunner_spire_pack_1"
- Contains Spellguard Magus (NPC 232113) with Spellguard's Protection (Spell 1253686)
- Ability entry has both first_cast (50) and cooldown (50) fields (DATA-02 satisfied)
- Schema structure supports future expansion to multiple mobs/abilities per pack
  </done>
</task>

</tasks>

<verification>
Run all checks from project root:
1. `test -f TerriblePackWarnings.toc` -- TOC exists
2. `test -f Core.lua` -- Core exists
3. `test -f Data/WindrunnerSpire.lua` -- Data file exists
4. `grep "120000, 120001" TerriblePackWarnings.toc` -- dual interface version
5. `grep "Core.lua" TerriblePackWarnings.toc` -- Core.lua in load order
6. `grep "WindrunnerSpire" TerriblePackWarnings.toc` -- data file in load order
7. `grep "ns.PackDatabase = ns.PackDatabase or {}" Core.lua` -- module-scope init (critical for load order)
8. `grep "first_cast" Data/WindrunnerSpire.lua && grep "cooldown" Data/WindrunnerSpire.lua` -- DATA-02 fields present
</verification>

<success_criteria>
- All three files exist and contain the specified content
- TOC has correct Interface version, SavedVariables, and file load order
- Core.lua initializes PackDatabase at module scope (before event handlers)
- Data file populates PackDatabase with correct pack key, mob, and ability data
- Every ability entry has both first_cast and cooldown fields
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data/01-01-SUMMARY.md`
</output>
