---
phase: 19-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Data/WindrunnerSpire.lua
  - Data/AlgetharAcademy.lua
  - Data/MagistersTerrace.lua
  - Data/MaisaraCaverns.lua
  - Data/NexusPointXenas.lua
  - Data/PitOfSaron.lua
  - Data/SeatoftheTriumvirate.lua
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Every AbilityDB entry in all 8 non-Skyreach dungeon files has mobCategory = unknown"
    - "No mobClass field exists in any Data/*.lua file after both plans complete"
    - "Header comments in all 8 files document the mobCategory vocabulary and distinguish it from mobClass"
  artifacts:
    - path: "Data/WindrunnerSpire.lua"
      provides: "WindrunnerSpire AbilityDB with mobCategory = unknown for all 30 entries"
      contains: "mobCategory"
    - path: "Data/AlgetharAcademy.lua"
      provides: "AlgetharAcademy AbilityDB with mobCategory = unknown for all 16 entries"
      contains: "mobCategory"
    - path: "Data/MagistersTerrace.lua"
      provides: "MagistersTerrace AbilityDB with mobCategory = unknown for all 21 entries"
      contains: "mobCategory"
    - path: "Data/MaisaraCaverns.lua"
      provides: "MaisaraCaverns AbilityDB with mobCategory = unknown for all 30 entries"
      contains: "mobCategory"
    - path: "Data/NexusPointXenas.lua"
      provides: "NexusPointXenas AbilityDB with mobCategory = unknown for all 29 entries"
      contains: "mobCategory"
    - path: "Data/PitOfSaron.lua"
      provides: "PitOfSaron AbilityDB with mobCategory = unknown for all 22 entries"
      contains: "mobCategory"
    - path: "Data/SeatoftheTriumvirate.lua"
      provides: "SeatoftheTriumvirate AbilityDB with mobCategory = unknown for all 18 entries"
      contains: "mobCategory"
  key_links:
    - from: "Data/WindrunnerSpire.lua"
      to: "Engine/NameplateScanner.lua"
      via: "ns.AbilityDB[npcID].mobCategory read at runtime"
      pattern: "mobCategory"
---

<objective>
Replace mobClass with mobCategory = "unknown" in all 8 non-Skyreach dungeon data files and update header comments.

Purpose: Complete the AbilityDB schema migration so every mob in every dungeon has an explicit mobCategory field. Non-Skyreach dungeons default to "unknown" which acts as a wildcard at runtime (Phase 20).
Output: 8 Data/*.lua files with mobCategory = "unknown" on every entry and updated header comments.
</objective>

<execution_context>
@C:/Users/jonat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jonat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-layer/19-CONTEXT.md
@.planning/phases/19-data-layer/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mobClass with mobCategory = "unknown" in all 8 non-Skyreach data files</name>
  <files>Data/WindrunnerSpire.lua, Data/AlgetharAcademy.lua, Data/MagistersTerrace.lua, Data/MaisaraCaverns.lua, Data/NexusPointXenas.lua, Data/PitOfSaron.lua, Data/SeatoftheTriumvirate.lua</files>
  <read_first>
    - Data/WindrunnerSpire.lua (current state — 30 entries with mobClass = "WARRIOR")
    - Data/AlgetharAcademy.lua (current state — 16 entries)
    - Data/MagistersTerrace.lua (current state — 21 entries)
    - Data/MaisaraCaverns.lua (current state — 30 entries)
    - Data/NexusPointXenas.lua (current state — 29 entries)
    - Data/PitOfSaron.lua (current state — 22 entries)
    - Data/SeatoftheTriumvirate.lua (current state — 18 entries)
  </read_first>
  <action>
For each of the 8 non-Skyreach data files, apply these identical changes:

**A. Replace the file header comment.** Each file currently has a header like:
```
-- <DungeonName> ability database
-- Keyed by npcID; mobClass defaults to WARRIOR, abilities from MDT spells
```

Replace with (substituting the dungeon name):
```lua
-- <DungeonName> ability database
-- Keyed by npcID
-- mobCategory: semantic role — "boss"|"miniboss"|"caster"|"warrior"|"rogue"|"trivial"|"unknown"
--   (not to be confused with the runtime WoW class token e.g. "WARRIOR"; that is never stored here)
```

NOTE: Some files may have a slightly different header format (e.g., `Data/WindrunnerSpire.lua` says "Windrunner Spire ability database"). Keep the dungeon name as-is from the existing header; only change the description lines.

**B. In every AbilityDB entry, replace:**
```lua
    mobClass = "WARRIOR",
```
with:
```lua
    mobCategory = "unknown",  -- semantic role; see header for vocabulary
```

This is a mechanical find-and-replace. Every entry in every non-Skyreach file uses `mobClass = "WARRIOR"` — replace all of them with `mobCategory = "unknown"`.

**C. Expected entry counts per file (verify after editing):**

| File | Expected entries |
|------|----------------|
| WindrunnerSpire.lua | 30 |
| AlgetharAcademy.lua | 16 |
| MagistersTerrace.lua | 21 |
| MaisaraCaverns.lua | 30 |
| NexusPointXenas.lua | 29 |
| PitOfSaron.lua | 22 |
| SeatoftheTriumvirate.lua | 18 |
| **Total** | **166** |

Do NOT add any new stub entries for non-Skyreach dungeons. Only edit existing entries.

**D. After editing all files, run:** `grep -rn "mobClass" Data/` — must return zero results across all Data/*.lua files (this covers both this plan's files and Plan 01's Skyreach.lua if already completed).
  </action>
  <verify>
    <automated>cd C:/Users/jonat/Repositories/TerriblePackWarnings && grep -c "mobClass" Data/WindrunnerSpire.lua Data/AlgetharAcademy.lua Data/MagistersTerrace.lua Data/MaisaraCaverns.lua Data/NexusPointXenas.lua Data/PitOfSaron.lua Data/SeatoftheTriumvirate.lua | grep -v ":0$" | wc -l | grep "^0$" && grep -c "mobCategory" Data/WindrunnerSpire.lua | grep "^30$" && grep -c "mobCategory" Data/AlgetharAcademy.lua | grep "^16$" && grep -c "mobCategory" Data/MagistersTerrace.lua | grep "^21$" && grep -c "mobCategory" Data/MaisaraCaverns.lua | grep "^30$" && grep -c "mobCategory" Data/NexusPointXenas.lua | grep "^29$" && grep -c "mobCategory" Data/PitOfSaron.lua | grep "^22$" && grep -c "mobCategory" Data/SeatoftheTriumvirate.lua | grep "^18$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mobClass" Data/WindrunnerSpire.lua` returns `0`
    - `grep -c "mobClass" Data/AlgetharAcademy.lua` returns `0`
    - `grep -c "mobClass" Data/MagistersTerrace.lua` returns `0`
    - `grep -c "mobClass" Data/MaisaraCaverns.lua` returns `0`
    - `grep -c "mobClass" Data/NexusPointXenas.lua` returns `0`
    - `grep -c "mobClass" Data/PitOfSaron.lua` returns `0`
    - `grep -c "mobClass" Data/SeatoftheTriumvirate.lua` returns `0`
    - `grep -c "mobCategory" Data/WindrunnerSpire.lua` returns `30`
    - `grep -c "mobCategory" Data/AlgetharAcademy.lua` returns `16`
    - `grep -c "mobCategory" Data/MagistersTerrace.lua` returns `21`
    - `grep -c "mobCategory" Data/MaisaraCaverns.lua` returns `30`
    - `grep -c "mobCategory" Data/NexusPointXenas.lua` returns `29`
    - `grep -c "mobCategory" Data/PitOfSaron.lua` returns `22`
    - `grep -c "mobCategory" Data/SeatoftheTriumvirate.lua` returns `18`
    - Every `mobCategory` line in all 7 files contains the value `"unknown"` (no other category values)
    - Each file header contains the string `semantic role` and does NOT contain `mobClass defaults to`
  </acceptance_criteria>
  <done>All 8 non-Skyreach data files have mobCategory = "unknown" on every entry, zero mobClass references, and updated header comments documenting the vocabulary</done>
</task>

</tasks>

<verification>
- `grep -rn "mobClass" Data/` returns zero results across ALL Data/*.lua files
- `grep -c "mobCategory" Data/WindrunnerSpire.lua` returns 30
- `grep -c "mobCategory" Data/AlgetharAcademy.lua` returns 16
- `grep -c "mobCategory" Data/MagistersTerrace.lua` returns 21
- `grep -c "mobCategory" Data/MaisaraCaverns.lua` returns 30
- `grep -c "mobCategory" Data/NexusPointXenas.lua` returns 29
- `grep -c "mobCategory" Data/PitOfSaron.lua` returns 22
- `grep -c "mobCategory" Data/SeatoftheTriumvirate.lua` returns 18
- Total: 166 entries across 7 files (8th dungeon file, Skyreach, handled by Plan 01)
</verification>

<success_criteria>
- Zero occurrences of `mobClass` in any of the 8 non-Skyreach data files
- 166 total mobCategory entries across 7 files (8th file is an alias check — there is no 8th file in this plan's scope; 7 files total)
- Every mobCategory value is exactly `"unknown"` (lowercase, no other values)
- All file headers updated with mobCategory vocabulary documentation
</success_criteria>

<output>
After completion, create `.planning/phases/19-data-layer/19-02-SUMMARY.md`
</output>
