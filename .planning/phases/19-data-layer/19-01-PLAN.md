---
phase: 19-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Data/Skyreach.lua
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04

must_haves:
  truths:
    - "Every Skyreach AbilityDB entry has a mobCategory field with a valid lowercase value"
    - "All 22 Skyreach mobs from DungeonEnemies[151] have an AbilityDB entry with the correct category from the reference table"
    - "No mobClass field exists anywhere in Skyreach.lua"
    - "Outcast Servant (npcID 75976) has a stub entry with mobCategory = warrior and empty abilities"
  artifacts:
    - path: "Data/Skyreach.lua"
      provides: "Skyreach AbilityDB with mobCategory field for all 22 mobs"
      contains: "mobCategory"
  key_links:
    - from: "Data/Skyreach.lua"
      to: "Engine/NameplateScanner.lua"
      via: "ns.AbilityDB[npcID].mobCategory read at runtime"
      pattern: "mobCategory"
---

<objective>
Replace mobClass with per-mob mobCategory values in Data/Skyreach.lua and add a stub entry for Outcast Servant (npcID 75976).

Purpose: Skyreach is the pilot dungeon for the mob category system. All 22 mobs receive their correct semantic role category from the reference table, establishing the data pattern for all dungeons.
Output: Data/Skyreach.lua with 22 AbilityDB entries, each containing mobCategory instead of mobClass.
</objective>

<execution_context>
@C:/Users/jonat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jonat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-layer/19-CONTEXT.md
@.planning/phases/19-data-layer/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mobClass with mobCategory in Skyreach.lua and add Outcast Servant stub</name>
  <files>Data/Skyreach.lua</files>
  <read_first>
    - Data/Skyreach.lua (current state — 21 entries, all with mobClass = "WARRIOR")
    - Data/DungeonEnemies.lua (cross-reference: search for dungeonIdx 151 to confirm all 22 npcIDs)
  </read_first>
  <action>
Edit Data/Skyreach.lua with these exact changes:

1. **Replace the file header comment** (lines 5-6) with:
```lua
-- Skyreach ability database
-- Keyed by npcID
-- mobCategory: semantic role — "boss"|"miniboss"|"caster"|"warrior"|"rogue"|"trivial"|"unknown"
--   (not to be confused with the runtime WoW class token e.g. "WARRIOR"; that is never stored here)
```

2. **Replace `mobClass = "WARRIOR"` with `mobCategory = "<value>"` in every existing entry** using these exact assignments:

| npcID | mobCategory value |
|-------|-------------------|
| 76132 | `"warrior"` |
| 78932 | `"caster"` |
| 250992 | `"rogue"` |
| 79462 | `"caster"` |
| 79466 | `"caster"` |
| 79467 | `"caster"` |
| 78933 | `"miniboss"` |
| 76087 | `"miniboss"` |
| 79093 | `"trivial"` |
| 76154 | `"miniboss"` |
| 75964 | `"boss"` |
| 76141 | `"boss"` |
| 76142 | `"unknown"` |
| 76143 | `"boss"` |
| 76149 | `"miniboss"` |
| 76205 | `"warrior"` |
| 76227 | `"unknown"` |
| 76266 | `"boss"` |
| 76285 | `"unknown"` |
| 79303 | `"miniboss"` |
| 251880 | `"trivial"` |

Each line should read: `    mobCategory = "value",  -- semantic role; see header for vocabulary`

3. **Add a new stub entry for Outcast Servant** (npcID 75976). Place it in npcID order among the other entries:
```lua
-- Outcast Servant (75976) -- in DungeonEnemies, no tracked abilities
ns.AbilityDB[75976] = {
    mobCategory = "warrior",  -- semantic role; see header for vocabulary
    abilities = {},
}
```

4. **Verify completeness:** After editing, the file must contain exactly 22 `ns.AbilityDB[` assignments. Cross-reference against DungeonEnemies[151] which has 22 entries.
  </action>
  <verify>
    <automated>cd C:/Users/jonat/Repositories/TerriblePackWarnings && grep -c "mobClass" Data/Skyreach.lua | grep "^0$" && grep -c "mobCategory" Data/Skyreach.lua | grep "^22$" && grep -c "ns.AbilityDB\[" Data/Skyreach.lua | grep "^22$" && grep "75976" Data/Skyreach.lua | grep "warrior" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mobClass" Data/Skyreach.lua` returns `0` (no mobClass references remain)
    - `grep -c "mobCategory" Data/Skyreach.lua` returns `22` (one per entry, all present)
    - `grep -c "ns.AbilityDB\[" Data/Skyreach.lua` returns `22` (all 22 mobs including stub)
    - `grep "75976" Data/Skyreach.lua` returns a line containing `mobCategory = "warrior"`
    - `grep "78932" Data/Skyreach.lua` returns a line containing `mobCategory = "caster"` (spot check)
    - `grep "75964" Data/Skyreach.lua` returns a line containing `mobCategory = "boss"` (spot check)
    - `grep "79093" Data/Skyreach.lua` returns a line containing `mobCategory = "trivial"` (spot check)
    - File header contains the string `semantic role` and does NOT contain `mobClass defaults to`
  </acceptance_criteria>
  <done>Skyreach.lua contains 22 AbilityDB entries with correct per-mob mobCategory values, zero mobClass references, and updated header comment</done>
</task>

</tasks>

<verification>
- `grep -c "mobClass" Data/Skyreach.lua` returns 0
- `grep -c "ns.AbilityDB\[" Data/Skyreach.lua` returns 22
- All 22 npcIDs from the reference table are present with correct categories
- Header comment documents mobCategory vocabulary and distinguishes from mobClass
</verification>

<success_criteria>
- Zero occurrences of `mobClass` in Data/Skyreach.lua
- 22 AbilityDB entries including npcID 75976 stub
- Each entry has the correct mobCategory value per the reference table
- Header comment explicitly names the field distinction
</success_criteria>

<output>
After completion, create `.planning/phases/19-data-layer/19-01-SUMMARY.md`
</output>
