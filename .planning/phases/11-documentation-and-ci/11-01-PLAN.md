---
phase: 11-documentation-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - TerriblePackWarnings.toc
  - .pkgmeta
  - .github/workflows/release.yml
autonomous: false
requirements: [DOC-01, DOC-02, CI-01, CI-02]

must_haves:
  truths:
    - "A visitor to the GitHub repo sees a README with project description, WIP notice, features, usage, gallery, known issues, AI disclosure, and license"
    - "The TOC file has CurseForge project ID 1487612, Wago ID ZKbxadN1, and blp icon texture"
    - "The release workflow and .pkgmeta match TerribleBuffTracker patterns"
    - "The release script produces a working release package"
  artifacts:
    - path: "README.md"
      provides: "Project documentation"
      contains: "TerriblePackWarnings"
    - path: "TerriblePackWarnings.toc"
      provides: "Addon metadata with project IDs and icon"
      contains: "X-Curse-Project-ID: 1487612"
    - path: ".pkgmeta"
      provides: "Release package configuration"
      contains: "package-as: TerriblePackWarnings"
    - path: ".github/workflows/release.yml"
      provides: "CI release workflow"
      contains: "BigWigsMods/packager@v2"
  key_links:
    - from: "TerriblePackWarnings.toc"
      to: "tpw_64x64.blp"
      via: "IconTexture field"
      pattern: "IconTexture.*tpw_64x64"
    - from: ".github/workflows/release.yml"
      to: ".pkgmeta"
      via: "BigWigsMods packager reads .pkgmeta"
      pattern: "BigWigsMods/packager"
    - from: "scripts/release.bat"
      to: ".github/workflows/release.yml"
      via: "Tag push triggers workflow"
      pattern: "git.*push.*origin"
---

<objective>
Create README.md, update TOC with project IDs and icon, and verify CI/release files are correct.

Purpose: Make the addon presentable on GitHub and releasable via CI.
Output: README.md, updated TOC, verified CI pipeline.
</objective>

<execution_context>
@C:/Users/jonat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jonat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentation-and-ci/11-CONTEXT.md

Reference files (read these to match style/patterns):
@C:/Users/jonat/Repositories/TerribleBuffTracker/README.md
@C:/Users/jonat/Repositories/TerribleBuffTracker/TerribleBuffTracker.toc
@C:/Users/jonat/Repositories/TerribleBuffTracker/.pkgmeta
@C:/Users/jonat/Repositories/TerribleBuffTracker/.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README and update TOC/CI metadata</name>
  <files>README.md, TerriblePackWarnings.toc, .pkgmeta, .github/workflows/release.yml</files>
  <action>
**README.md** -- Create a new README following TerribleBuffTracker's style and tone. Read the TBT README at C:\Users\jonat\Repositories\TerribleBuffTracker\README.md as the style reference. The README must include these sections in order:

1. **Header**: `# TerriblePackWarnings` with a one-line description: dungeon trash pack ability warnings for WoW Midnight M+ (similar to TBT's opening line style)
2. **Description paragraph**: Casual tone explaining what it does -- shows timed ability warnings (spell icons with countdowns) when pulling trash packs, using imported MDT routes to know which mobs have dangerous abilities. Mention the "Terrible" naming pattern. Include a **WIP/unreleased notice** (bold, prominent) -- this addon is in early development and not yet published.
3. **AI Usage** section: Same disclosure pattern as TBT -- built with Claude AI, developer is experienced but new to WoW addon dev, maintained by the author. Keep the same casual/honest tone.
4. **Showcase** section: Single image reference `![Pack warnings in action](ws_fst_pack.png)`
5. **Features** section (bullet list):
   - Import MDT routes via paste (no slash command -- WoW chat buffer too small)
   - Per-pack ability warnings with spell icons and countdown timers
   - Automatic mob detection via nameplate scanning
   - Combat-aware: warnings activate when you pull tracked packs
   - Supports custom dungeon data packs
6. **Usage** section:
   - `/tpw` -- Open the pack management frame
   - Import a route: copy MDT export string, open TPW, click Import, paste
   - Warnings appear automatically when pulling packs with tracked abilities
7. **Known Issues and Limitations** section:
   - Nameplate scanning requires nameplates to be visible (enemy nameplates must be enabled)
   - Only supports dungeons with data packs (currently Windrunner's Spire/Flood Season Terminus)
   - 0.25s nameplate scan interval may miss very brief nameplate appearances
   - WIP: many features still in development
8. **License** section: `[WTFPL](LICENSE)`

**TerriblePackWarnings.toc** -- Add three lines after the existing `## Category: Combat` line (before `## SavedVariables`):
```
## X-Curse-Project-ID: 1487612
## X-Wago-ID: ZKbxadN1
## IconTexture: Interface\AddOns\TerriblePackWarnings\tpw_64x64
```

**CI verification** -- Read the existing .pkgmeta and .github/workflows/release.yml. Compare against the TBT references. The TPW files should already be correct:
- .pkgmeta: has `package-as: TerriblePackWarnings`, externals for LibStub/AceSerializer/LibDeflate, ignore list with .git, .gitignore, .github, .pkgmeta, .planning, CLAUDE.md, README.md, LICENSE, scripts, *.png
- release.yml: has tag trigger, GITHUB_OAUTH, BigWigsMods/packager@v2, commented-out CF_API_KEY and WAGO_API_TOKEN

If these already match TBT patterns (they should), no changes needed. If any discrepancy found, fix to match.
  </action>
  <verify>
    <automated>bash -c "grep -q 'X-Curse-Project-ID: 1487612' TerriblePackWarnings.toc && grep -q 'X-Wago-ID: ZKbxadN1' TerriblePackWarnings.toc && grep -q 'IconTexture.*tpw_64x64' TerriblePackWarnings.toc && grep -q 'TerriblePackWarnings' README.md && grep -q 'WIP\|unreleased\|early development' README.md && grep -q 'WTFPL' README.md && grep -q 'ws_fst_pack' README.md && grep -q 'AI' README.md && echo 'PASS: All checks passed' || echo 'FAIL: Missing content'"</automated>
  </verify>
  <done>README.md exists with all required sections (description, WIP notice, AI usage, showcase, features, usage, known issues, license). TOC has CurseForge ID 1487612, Wago ID ZKbxadN1, and blp icon path. CI files verified against TBT patterns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews README and verifies CI readiness</name>
  <files>README.md</files>
  <action>
Present the completed README.md, updated TOC, and CI file status to the user for review. The user will read the README, suggest edits, and confirm everything looks correct. Apply any requested changes.
  </action>
  <verify>User confirms approval</verify>
  <done>User has approved README content and CI configuration</done>
  <what-built>README.md created with project description, WIP notice, features, usage, gallery screenshot, known issues, AI disclosure, and WTFPL license. TOC updated with CurseForge/Wago project IDs and icon texture. CI files (.pkgmeta, release.yml, release.bat) verified against TerribleBuffTracker patterns.</what-built>
  <how-to-verify>
    1. Open README.md and review content, tone, and completeness
    2. Check TerriblePackWarnings.toc has the three new metadata lines (X-Curse-Project-ID, X-Wago-ID, IconTexture)
    3. If you want to test the release script: `./scripts/release.bat 0.0.4-test` (this will create and push a tag -- only do this if you want to trigger a CI run)
    4. Provide any README edits you want, or type "approved" to continue
  </how-to-verify>
  <resume-signal>Type "approved" or describe README changes needed</resume-signal>
</task>

</tasks>

<verification>
- README.md contains all 8 required sections
- TOC has X-Curse-Project-ID: 1487612
- TOC has X-Wago-ID: ZKbxadN1
- TOC has IconTexture pointing to tpw_64x64 blp
- .pkgmeta excludes all non-addon files
- release.yml uses BigWigsMods/packager@v2
- release.bat creates tag and pushes
</verification>

<success_criteria>
- README is complete, casual in tone, and has WIP notice
- TOC metadata matches CurseForge/Wago project IDs
- CI pipeline is ready for first release
- User has reviewed and approved README content
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-and-ci/11-01-SUMMARY.md`
</output>
