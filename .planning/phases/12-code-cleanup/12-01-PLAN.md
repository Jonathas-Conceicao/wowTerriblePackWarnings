---
phase: 12-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Engine/Scheduler.lua
  - Engine/NameplateScanner.lua
  - Engine/CombatWatcher.lua
  - Display/IconDisplay.lua
  - Core.lua
  - Import/Decode.lua
  - Import/Pipeline.lua
  - Data/WindrunnerSpire.lua
  - Data/DungeonEnemies.lua
  - UI/PackFrame.lua
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04]

must_haves:
  truths:
    - "No DEBUG flags or dbg() calls exist in any shipped Lua file"
    - "The /tpw slash handler has no show or hide debug subcommands"
    - "No unused local/global variables remain in any Lua file"
    - "The 0.25s nameplate scanner tick has inline comments documenting cost and review status"
    - "Any other game-loop hot paths have inline cost/review comments"
  artifacts:
    - path: "Engine/Scheduler.lua"
      provides: "Clean scheduler with no debug code"
      contains: "-- no DEBUG flag"
    - path: "Engine/NameplateScanner.lua"
      provides: "Clean scanner with hot-path audit comments"
      contains: "PERF"
    - path: "Display/IconDisplay.lua"
      provides: "Clean icon display with no debug code"
      contains: "-- no DEBUG flag"
    - path: "Core.lua"
      provides: "Slash handler with no debug subcommands"
      contains: "-- no show/hide debug commands"
  key_links:
    - from: "Engine/NameplateScanner.lua"
      to: "0.25s tick loop"
      via: "OnUpdate handler"
      pattern: "PERF.*0\\.25"
---

<objective>
Remove all debug artifacts, dead code, and audit hot paths across the entire addon codebase.

Purpose: Ship clean production code for v0.0.4 with no debug noise, no dead code, and documented hot paths.
Output: All 10 Lua files cleaned; hot paths annotated with PERF comments.
</objective>

<execution_context>
@C:/Users/jonat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jonat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Source files (ALL must be read during execution):
@Core.lua
@Engine/Scheduler.lua
@Engine/CombatWatcher.lua
@Engine/NameplateScanner.lua
@Display/IconDisplay.lua
@Import/Decode.lua
@Import/Pipeline.lua
@Data/WindrunnerSpire.lua
@Data/DungeonEnemies.lua
@UI/PackFrame.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove debug artifacts and debug slash commands</name>
  <files>Engine/Scheduler.lua, Engine/NameplateScanner.lua, Display/IconDisplay.lua, Core.lua</files>
  <action>
CLEAN-01 -- Remove DEBUG flags and dbg() from three files:

1. **Engine/Scheduler.lua** (lines 18-19):
   - Delete `local DEBUG = true` (line 18)
   - Delete `local function dbg(msg) ... end` definition (line 19, extends ~2 lines)
   - Delete all `dbg(...)` calls: lines 45, 53, 63, 97, 105, 133
   - Verify no orphan references to DEBUG or dbg remain

2. **Engine/NameplateScanner.lua** (lines 25-26):
   - Delete `local DEBUG = true` (line 25)
   - Delete `local function dbg(msg) ... end` definition (line 26, extends ~2 lines)
   - Delete all `dbg(...)` calls: lines 48, 55, 72, 83, 112, 146, 170
   - Verify no orphan references to DEBUG or dbg remain

3. **Display/IconDisplay.lua** (lines 6-7):
   - Delete `local DEBUG = true` (line 6)
   - Delete `local function dbg(msg) ... end` definition (line 7, extends ~2 lines)
   - Delete all `dbg(...)` calls: lines 55, 58, 117, 192, 205, 227, 239, 265, 279
   - Verify no orphan references to DEBUG or dbg remain

CLEAN-02 -- Remove debug slash commands from Core.lua:

4. **Core.lua** (lines 72+, 94+):
   - Remove the `elseif cmd == "show"` branch (line 72 and its body)
   - Remove the `elseif cmd == "hide"` branch (line 94 and its body)
   - Do NOT remove legitimate slash commands (status, reset, etc.)
   - Ensure the remaining elseif chain is syntactically valid

After all edits, do a final grep across ALL .lua files (excluding Libs/) for `DEBUG`, `dbg(`, `"show"`, `"hide"` to confirm zero matches in the slash handler context.
  </action>
  <verify>
    <automated>cd /c/Users/jonat/Repositories/TerriblePackWarnings && grep -rn "local DEBUG\|local function dbg\|dbg(" Engine/Scheduler.lua Engine/NameplateScanner.lua Display/IconDisplay.lua && echo "FAIL: debug artifacts remain" || echo "PASS: no debug artifacts" && grep -n '"show"\|"hide"' Core.lua | grep -v "-- " && echo "FAIL: debug commands remain" || echo "PASS: no debug commands"</automated>
  </verify>
  <done>Zero DEBUG flags, zero dbg() definitions, zero dbg() calls across all three engine/display files. Zero show/hide debug subcommands in Core.lua slash handler. All files parse correctly (no syntax errors from incomplete elseif chains).</done>
</task>

<task type="auto">
  <name>Task 2: Dead code removal and hot path audit</name>
  <files>Core.lua, Engine/Scheduler.lua, Engine/CombatWatcher.lua, Engine/NameplateScanner.lua, Display/IconDisplay.lua, Import/Decode.lua, Import/Pipeline.lua, Data/WindrunnerSpire.lua, Data/DungeonEnemies.lua, UI/PackFrame.lua</files>
  <action>
CLEAN-03 -- Dead code removal:

Read EVERY Lua file listed above (all 10). For each file, analyze:

1. **Unused locals**: Variables declared with `local` but never referenced after assignment. Remove them.
2. **Unused globals**: Variables assigned to global scope (no `local` keyword) but never read. Remove or convert to local if used only within the file.
3. **Single-use wrapper functions**: Functions defined and called exactly once, where inlining the body at the call site would be clearer. Inline them and remove the definition. Exception: do NOT inline functions that improve readability of complex logic or serve as named documentation.
4. **Commented-out code blocks**: Remove dead commented-out code (not documentation comments -- only old code).
5. **Unreachable code**: Code after unconditional return statements.

For each removal, ensure no other file references the removed symbol. Check cross-file usage patterns:
- Globals used across files (e.g., addon table fields like `TPW.Something`) must NOT be removed
- Only remove truly orphaned code

CLEAN-04 -- Hot path audit:

Identify and annotate ALL game-loop hot paths with `-- PERF:` comments:

1. **NameplateScanner.lua:Tick** (the 0.25s OnUpdate callback):
   - Add comment block above the function documenting: tick interval, what it does per tick (iterate nameplates, UnitGUID checks), estimated cost (number of API calls per tick), and review status
   - Example: `-- PERF: Runs every 0.25s. Iterates C_NamePlate.GetNamePlates() (typically 5-20 frames), calls UnitGUID per frame. Cost: ~20 API calls/tick. Reviewed 2026-03-16, acceptable for current scope.`

2. **Any other OnUpdate handlers** found in any file -- apply same PERF comment pattern

3. **Any event handlers that fire at high frequency** (UNIT_AURA, COMBAT_LOG_EVENT_UNFILTERED, etc.) -- add PERF comment if found

For each hot path, document:
- Trigger frequency
- Work done per invocation
- Number of WoW API calls
- Review status and date

Report findings as a comment in the SUMMARY (which hot paths exist, their assessed cost).
  </action>
  <verify>
    <automated>cd /c/Users/jonat/Repositories/TerriblePackWarnings && grep -rn "PERF:" Engine/NameplateScanner.lua && echo "PASS: hot path documented" || echo "FAIL: no PERF comments in NameplateScanner"</automated>
  </verify>
  <done>All unused variables and dead code removed across all 10 Lua files. All game-loop hot paths annotated with PERF comments documenting frequency, cost, and review status. NameplateScanner:Tick has a detailed cost annotation. SUMMARY includes a hot path findings report.</done>
</task>

</tasks>

<verification>
1. `grep -rn "local DEBUG\|dbg(" --include="*.lua" . | grep -v Libs/` returns zero matches
2. `grep -n '"show"\|"hide"' Core.lua` returns zero matches in slash handler context
3. `grep -rn "PERF:" --include="*.lua" .` returns at least one match in NameplateScanner.lua
4. All Lua files load without syntax errors (no broken elseif chains, no missing end statements)
</verification>

<success_criteria>
- No DEBUG flags, dbg() calls, or debug slash commands exist in any shipped Lua file
- No unused variables or dead wrapper functions remain
- Every game-loop hot path has a PERF comment with frequency, cost estimate, and review date
- All files are syntactically valid Lua
</success_criteria>

<output>
After completion, create `.planning/phases/12-code-cleanup/12-01-SUMMARY.md`
</output>
